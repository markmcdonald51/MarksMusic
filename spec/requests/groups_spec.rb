require 'spec_helper'

describe "Groups" do
  describe "GET /groups" do
    it "works! (now write some real specs)" do
      get groups_path
      expect(response.status).to be(200)
    end
  end
end
